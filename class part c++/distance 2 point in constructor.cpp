#include <iostream>
#include <math.h>
using namespace std;

class point
{
    int x, y, distance;

public:
    point(int a, int b)
    {
        x = a;
        y = b;
    }
    void printdata()
    {
        cout << "The distance is two line " << distance;
    }
    /*  void getdata(){
        cout<<"enter the two data" >>x>>y;
      }*/
    int dis(int w, int z)
    {

        int distance = sqrt(((5 - 10) * (5 - 10)) + ((10 - 5) * (10 - 5)));
    }
};

int main()
{
    point p1(5, 10), p2(10, 5), p3;

    p3.

        p3.printdata();

    return 0;
}
